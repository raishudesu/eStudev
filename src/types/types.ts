export type TFeatureCard = {
  icon: string;
  characteristic: string;
  description: string;
};

export type TThread = {
  title: string;
  category: string[];
  content: string;
  authorName: string;
};
